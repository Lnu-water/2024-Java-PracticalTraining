package Learn.day01.NumericSorting;

public class Maximum {
    public <T> T maximun(T values) {
        T max_value = values.;
        for (T value : values) {
            value > max_value ? max_value : value;
        }
        return max_value;
    }
}